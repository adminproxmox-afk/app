# Skill Play Web App

Static HTML/CSS/JS app in the repository root, plus a React/Vite prototype in
`react-app`.

## React prototype

```sh
cd react-app
npm install
npm run dev
npm run build
```

The GitHub Pages workflow currently builds and deploys `react-app/dist`.
